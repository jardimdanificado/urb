#include "../etc/syntax.h"

// we need this to prevent urb from interpreting our local scope
goto(start)

// there is no reason to pollute our global scope(context)
// lets store our local variables here in the code
local:
    a: 50
    b: 123
start:
    call(print, $b)
    call(println)
    
    call(print, $a)
    call(println)
    
    call(print, start)
    call(println)

    call(print, start)
    call(println)

call(ls, _mem)