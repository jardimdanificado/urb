#include "../etc/syntax.h"

// we need this to prevent urb from interpreting our variables
goto(start)

local:
    i: 0
start:
    call(print, $i)
    call(println)
    call(set, _mem, i, call(inc, $i))
    call(print, call(get, _mem, i))
    call(println)
    call(jif, call(le, $i, 100000), start)