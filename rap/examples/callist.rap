define(callist, {{interpret(exec, $1, stack)}})


data [
    i: 0
    str: "hello world!\n"
    list: 0
    test_proc {
        pring(pointer(mem, sub(str, data)))
    }
]

main {
    set(mem, list, sublist(mem, data, sub(main, data)))
    callist($list)
}

end {
    free($list)
}